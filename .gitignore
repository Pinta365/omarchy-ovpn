# Python
__pycache__/
*.pyc

# Local development only: tools/reload.sh syncs the tree into the shell,
# knowledge/ is the lab notebook (roadmap, findings) shared between agents.
# Durable facts belong in README.md instead.
tools/
knowledge/

# Editor / agent / OS clutter
.claude/
.vscode/
.idea/
*.swp
*~
.DS_Store

# Files coding agents load on their own. `omarchy plugin add` clones the whole
# repo into users' plugin folders, so these must never ship; they stay local.
AGENTS.md
CLAUDE.md
CLAUDE.local.md
GEMINI.md
.cursorrules
.windsurfrules
.clinerules
.cursor/
